---
import Layout from "../layouts/Layout.astro";
import PopupDemo from "../components/islands/PopupDemo.svelte";
---

<Layout title="Всплывающие окна" description="Демо попапов Telegram">
  <main class="min-h-[100dvh] px-4 pt-6 sm:px-6 md:px-8 pb-[calc(24px+env(safe-area-inset-bottom))]">
    <div class="mx-auto max-w-3xl">
      <a href="/" class="inline-flex items-center gap-2 text-sm text-[var(--color-text-secondary)] hover:text-white transition">
        ← Назад на главную
      </a>

      <header class="mt-5">
        <h1 class="text-3xl sm:text-4xl font-extrabold tracking-[-0.02em]">
          Всплывающие окна Telegram
        </h1>
        <p class="mt-3 text-[15px] sm:text-base leading-relaxed text-[var(--color-text-secondary)]">
          Изучите нативную систему попапов Telegram с методами showPopup, showAlert и showConfirm.
          Эти окна появляются в интерфейсе Telegram с нативными стилями и анимациями.
        </p>
      </header>

      <section class="mt-8">
        <PopupDemo client:load />
      </section>
    </div>
  </main>
</Layout>
