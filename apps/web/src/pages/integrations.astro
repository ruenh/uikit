---
import Layout from "../layouts/Layout.astro";
import IntegrationsDemo from "../components/islands/IntegrationsDemo.svelte";
---

<Layout title="Интеграции" description="Авторизация и синхронизация настроек">
  <main class="min-h-[100dvh] px-4 pt-6 sm:px-6 md:px-8 pb-[calc(24px+env(safe-area-inset-bottom))]">
    <div class="mx-auto max-w-3xl">
      <a href="/" class="inline-flex items-center gap-2 text-sm text-[var(--color-text-secondary)] hover:text-white transition">
        ← Назад на главную
      </a>

      <header class="mt-5">
        <h1 class="text-3xl sm:text-4xl font-extrabold tracking-[-0.02em]">
          Интеграции с бэкендом
        </h1>
        <p class="mt-3 text-[15px] sm:text-base leading-relaxed text-[var(--color-text-secondary)]">
          Авторизуйтесь через Telegram initData и синхронизируйте настройки пользователя с базой данных.
          Ваша тема и настройки сохраняются между сессиями.
        </p>
      </header>

      <section class="mt-8">
        <IntegrationsDemo client:load />
      </section>
    </div>
  </main>
</Layout>
