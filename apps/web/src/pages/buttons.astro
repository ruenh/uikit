---
import Layout from "../layouts/Layout.astro";
import ButtonDemo from "../components/islands/ButtonDemo.svelte";
---

<Layout title="Кнопки" description="Демо MainButton и BackButton">
  <main class="min-h-[100dvh] px-4 pt-6 sm:px-6 md:px-8 pb-[calc(24px+env(safe-area-inset-bottom))]">
    <div class="mx-auto max-w-3xl">
      <a href="/" class="inline-flex items-center gap-2 text-sm text-[var(--color-text-secondary)] hover:text-white transition">
        ← Назад на главную
      </a>

      <header class="mt-5">
        <h1 class="text-3xl sm:text-4xl font-extrabold tracking-[-0.02em]">
          Кнопки Telegram
        </h1>
        <p class="mt-3 text-[15px] sm:text-base leading-relaxed text-[var(--color-text-secondary)]">
          Изучите MainButton и BackButton — нативные элементы UI Telegram, управляемые через WebApp API.
          Эти кнопки появляются в интерфейсе Telegram, а не в вашем веб-контенте.
        </p>
      </header>

      <section class="mt-8">
        <ButtonDemo client:load />
      </section>
    </div>
  </main>
</Layout>
